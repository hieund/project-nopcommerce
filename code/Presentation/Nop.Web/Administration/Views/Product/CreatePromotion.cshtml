@model PromotionModel
@{
    //page title
    ViewBag.Title = T("Admin.Catalog.Promotion.AddNew").Text;
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "promotion-form" }))
{
    <div class="section-header">
        <div class="title">
            <img src="@Url.Content("~/Administration/Content/images/ico-catalog.png")" alt="" />
            @T("Admin.Catalog.promotion.AddNew") @Html.ActionLink("(" + T("Admin.Catalog.promotion.BackToList") + ")", "PromotionList")
        </div>
        <div class="options">
            <input type="submit" name="save" class="t-button" value="@T("Admin.Common.Save")" />
            <input type="submit" name="save-continue" class="t-button" value="@T("Admin.Common.SaveContinue")" />
        </div>
    </div>
    @Html.Partial("_CreateOrUpdatePromotion", Model)
}